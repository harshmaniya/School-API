const Class = require('./classSchema')
const Result = require('./resultSchema')
const Role = require('./roleSchema')
const Subject = require('./subjectSchema')
const User = require('./userSchema')
const Topper = require('./topperSchema')

module.exports = {
    Class,
    Result,
    Role,
    Subject,
    User,
    Topper
}